version: 2.1
jobs:
   build:
     docker:
       - image: ubuntu
     steps:
       - checkout
       - run:
          name: "testing"
          shell: /bin/sh
          command: echo "Welcome"
            cd /tmp
